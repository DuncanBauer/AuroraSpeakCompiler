#ifndef COMPILER_H
#define COMPILER_H

#include <filesystem>
#include <fstream>
#include <iostream>
#include <string>

class Compiler
{
  public:
    Compiler() = default;

    void run();
    
  private:
};

#endif